# Troubleshooting

## Incident: Nginx Web Server Unavailable

### Problem

The hosted web application became unavailable and did not respond to HTTP requests.

### Symptoms

The web server was not responding to requests made using:

```bash
curl http://localhost
